### create a empty project and initialize variable
```
    1. $ conda create -p venv python==3.10
    2. $ conda activate venv/       ⇒ activate environment
    3. pip install -r requirements.txt

```
